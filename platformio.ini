; PlatformIO Project Configuration File
;
; Flash: pio run -e seeed_xiao_rp2350 -t upload
; Calibrate: ./run_calibration.sh   (no reflash required — uses HID feature reports)

[platformio]
src_dir = firmware/src
include_dir = firmware/include
lib_dir = firmware/lib
data_dir = firmware/data
test_dir = firmware/test

[env:seeed_xiao_rp2350]
platform = https://github.com/Seeed-Studio/platform-seeedboards.git
board = seeed-xiao-rp2350
framework = arduino
board_build.core = earlephilhower
board_build.arduino.earlephilhower.usb_product = CAD Mouse MK2
monitor_speed = 115200
build_flags =
	-DUSE_TINYUSB
lib_deps =
	infineon/XENSIV 3D Magnetic Sensor TLx493D@^1.1.0
	bxparks/AceButton@^1.10.1
	adafruit/Adafruit NeoPixel@^1.12.3
